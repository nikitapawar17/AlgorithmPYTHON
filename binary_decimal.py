from util.util_class import to_decimal


def main():
    binary = int(input("enter a no  : "))     # Here no present in 2's power
    print("Given no in decimal format : ", to_decimal(binary))


if __name__ == main():
    main()
